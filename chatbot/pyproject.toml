[project]
name = "securechat-chatbot"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "python-dotenv",
    "requests",
    "fastapi",
    "uvicorn",
]

[tool.vercel]
entrypoint = "web_server:app"
